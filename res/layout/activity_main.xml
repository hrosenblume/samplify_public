<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/the_whole_screen"
    android:background="@drawable/homebg"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
   
 
    
    <ImageButton
    android:id="@+id/record"
    android:background="@null"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="200dip"
    android:layout_width="220dip"
    android:layout_height="220dip"
    android:scaleType="fitCenter"
    android:onClick="fingerprint"
    android:padding="0dp"
    android:src="@drawable/recordselector" />
    
     <ImageView
    android:id="@+id/pressedrecord"
    android:background="@null"
    android:visibility="invisible"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="200dip"
    android:layout_width="220dip"
    android:layout_height="220dip"
    android:scaleType="fitCenter"
    android:padding="0dp"
    android:src="@drawable/pressedrecord" />
    
    <ImageButton
    android:id="@+id/button"
    android:background="@null"
    android:padding="0dp"
    android:layout_width="340dip"
    android:layout_height="50dip"
    android:scaleType="fitCenter"
    android:layout_alignParentBottom="true"
 	android:layout_centerHorizontal="true"
    android:layout_marginBottom="10dp"
    android:onClick="fromLibrary"
    android:src="@drawable/libraryselector" />
    
    <ImageButton
    android:id="@+id/muted_button"
    android:background="@null"
    android:padding="0dp"
    android:layout_width="340dip"
    android:layout_height="50dip"
    android:scaleType="fitCenter"
    android:layout_alignParentBottom="true"
 	android:layout_centerHorizontal="true"
    android:layout_marginBottom="10dp"
    android:src="@drawable/mutedbutton" />

</RelativeLayout>
